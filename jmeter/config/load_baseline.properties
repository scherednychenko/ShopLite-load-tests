# Load profile: baseline (repeatable benchmark)
threads=5
rampUpSec=60
durationSec=600

# Test parameters
runType=baseline
cartSize=10

# Think time (Uniform Random Timer): delay + random(range)
thinkTimeMinMs=300
thinkTimeRangeMs=900

# Timeouts (used by HTTP Request Defaults)
connectTimeoutMs=5000
responseTimeoutMs=30000
